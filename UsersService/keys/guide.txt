- Obtaining Keys:
Open bash and do these commands:

openssl genrsa -out private_key.pem 2048
openssl rsa -in private_key.pem -pubout -out public_key.pem

- Configuration:
Update your appsettings.json with the correct file paths for your private and public PEM keys